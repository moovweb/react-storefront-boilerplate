module.exports = {
  // Configures the creation of a service worker via workbox
  // See documentation here: https://developers.google.com/web/tools/workbox/modules/workbox-webpack-plugin
  // Note: in most cases you can leave this blank.  React Storefront allows you to configure client-side caching 
  // with the `cache` route handler in your router definition.  See src/routes.js for examples.
}